#!/usr/bin/env bash

# require kerberos username
if [[ -z "${1:-}" ]]; then
  echo "Usage: $0 <kerberos> [--restore-backup]"
  exit 1
fi

echo "hint: you can use --restore-backup optionally"

USER="$1"; shift

ssh -t "$USER@athena.dialup.mit.edu" "cd /mit/zbt && ./deploy_from_git.sh $*"
